CREATE TABLE suicides (
  id INTEGER PRIMARY KEY,
  country TEXT,
  year INTEGER,
  sex TEXT,
  age TEXT,
  num_of_sui INTEGER,
  age_population INTEGER,
  per_100k INTEGER,
  gdp_for_year TEXT,
  gdp_per_capita INTEGER,
  generation TEXT,
  population INTEGER
);